from django.db import models

# class Args(models.Model):
#     name = models.CharField(max_length=5000)
#     children = models.ManyToManyField("Args")
#     parent = models.ForeignKey("Args", on_delete=models.CASCADE)

#     def __str__(self):
#         return self.name
